# Основные зависимости учебного проекта
Django>=5.1,<5.2
Pillow>=10.0            # обработка изображений (ImageField, генерация заглушек)

# Драйвер PostgreSQL — нужен, только если DB_ENGINE=postgres.
# По умолчанию проект работает на SQLite и эта строка не требуется.
psycopg[binary]>=3.1 ; sys_platform != "win32"
