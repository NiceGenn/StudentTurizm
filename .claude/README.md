# Настройки для Claude Code

`settings.json` разрешает без лишних вопросов те команды, которые в этом
проекте выполняются постоянно: проверки Django, тесты, миграции, наполнение
базы, валидация справочника и чтение состояния git.

Рабочие правила проекта — в [`../CLAUDE.md`](../CLAUDE.md).
